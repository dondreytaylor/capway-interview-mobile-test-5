package app.capway.designchallenge.view.account

/**
 * Listener for events going back to the MainActivity.
 */
interface MainActivityListener {
    /**
     * General response to scroll events currently a Fling event.
     */
    fun onScrollEvent()
}